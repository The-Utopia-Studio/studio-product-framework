// Single source of truth for the product's display name — every homepage
// surface (nav logo, hero, footer) reads from here instead of hardcoding its
// own copy of the name, so customizeProduct() only has to edit one line.
export const PRODUCT_NAME = "Studio Product Framework";
